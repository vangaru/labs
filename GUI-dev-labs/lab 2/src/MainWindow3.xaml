<Window x:Class="lab2._3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:lab2._3"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Slider Minimum="8" Maximum="30" x:Name="SliderFontSize" Value="{Binding ElementName=tbox, Path=Text}" Margin="0,0,-0.4,0"></Slider>
        <TextBlock x:Name="Message" FontSize="{Binding ElementName=SliderFontSize, Path=Value}" Margin="0,40,-0.4,0">
            Dksaodfjeofjewiof
        </TextBlock>
        <TextBox x:Name="tbox" BorderThickness="5" Margin="0,101,684.6,251" Text="{Binding ElementName=SliderFontSize, Path=Value}"/>
    </Grid>
</Window>
